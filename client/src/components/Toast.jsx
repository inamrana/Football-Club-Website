import React from 'react';

// Simplified Toast - returning null as we implemented inline status for forms
// To expand later, we would use a context provider here to show global floating alerts
const Toast = () => {
  return null;
};

export default Toast;
